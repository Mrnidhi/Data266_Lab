# Shared Yelp Polarity data

Store the shared raw Yelp Polarity download here; huggingface/ holds the raw dataset cache. The PDF's directory illustration says IMDB, but the Task 2 instructions specify Yelp Polarity. Use Yelp consistently. Members keep their own vocabulary and processed splits in their own data_processed/ folders. Large raw data are ignored by Git.
