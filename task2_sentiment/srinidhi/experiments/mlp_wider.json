{
  "model": "maxpool_mlp",
  "hypothesis": "Test a larger scratch lexical baseline with reduced dropout.",
  "overrides": {
    "epochs": 12,
    "early_stopping_patience": 4,
    "num_workers": 2,
    "checkpoint_every_steps": 1000,
    "log_every_steps": 100,
    "embedding_dim": 256,
    "mlp_hidden": 128,
    "dropout": 0.2
  },
  "name": "mlp_wider"
}
