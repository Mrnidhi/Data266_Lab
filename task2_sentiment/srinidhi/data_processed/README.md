# Member-specific preprocessing

Keep this member's processed data separate from other members' data. Record how to reproduce it from the agreed raw dataset. Large generated data are ignored by Git; do not commit personal paths or credentials.
