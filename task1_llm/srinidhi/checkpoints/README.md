# Checkpoints

Full-run model weights are pending. Save the required weights and record the checkpoint hash, associated run ID and accessible download location here. Model binaries are ignored by ordinary Git. Include the actual required weights in the final Canvas submission and retain backups outside the training machine.
