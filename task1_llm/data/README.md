# Shared TinyStories data

Store the shared raw TinyStories download here. The code uses huggingface/ for the raw dataset cache. Keep original data unchanged and agree on its dataset revision. Each member's selected splits and preprocessing belong in their own data_processed/ folder. Large raw files are ignored by Git; this README keeps the required directory visible.
