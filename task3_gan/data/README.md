# Shared image data

The required domain directories are monet_jpg/ and photo_jpg/. Populate them only from the verified class competition data. Do not substitute a public tutorial competition silently. Preserve the raw images, and store each member's own split manifests and preprocessing under that member's data_processed/. Images are ignored by ordinary Git.
