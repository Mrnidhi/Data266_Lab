# A to B predictions

For Srinidhi's export convention, A = Photo and B = Monet. Write Photo-to-Monet predictions into a new run subdirectory here, for example pred_A2B/run-001/. The exporter requires a fresh directory to prevent mixing runs; do not use this README-containing parent as the export destination.
