# Outputs

Final full-run outputs are pending. Store generated samples, plots, predictions and metric files here, grouped by immutable run ID. The initial CPU verification evidence is under reproducibility/raw_logs/srinidhi at the repository root.
